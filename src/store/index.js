import { createStore } from "vuex";
import { currentPosts } from "@/currentPosts.js";

export default createStore({
  state: { currentPosts },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
